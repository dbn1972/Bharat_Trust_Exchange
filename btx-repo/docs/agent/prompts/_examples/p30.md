# P-30 — Example

## Filled invocation

```yaml
inputs:
  AUDIT_ID: SEC-2026-05-16
  SCOPE: control-plane
  REF: v0.7.0-beta.3
```

## findings.yaml (excerpt)

```yaml
findings:
  - id: SEC-001
    title: "BOLA on GET /v1/consents/{id} — operator can read other tenants' consents"
    severity: critical
    confidence: high
    owasp: API1:2023
    cwe: CWE-639
    files: ["services/control-plane/handlers/consents.go:142-178"]
    description: "Handler checks auth but not tenant_id ownership"
    evidence: |
      Handler reads `id` from path, fetches row, returns. No tenant scoping.
    fix: "Add tenant_id predicate in repo query; deny if mismatch; add CT-CP-097"
    verify: "CT-CP-097 + manual cross-tenant probe"
    owner: "@control-plane"
    eta: "2026-05-18"
  - id: SEC-007
    title: "Hardcoded HMAC key fallback in webhook signer"
    severity: high
    cwe: CWE-798
    files: ["services/control-plane/webhooks/sign.go:34"]
    fix: "Remove fallback; fail-closed; rotate via P-18"
  - id: SEC-014
    title: "AI-pattern: fake auth check in admin handler"
    severity: high
    notes: "Function `isAdmin()` returns true if `header == 'admin'`; looks like a check, isn't one"
    cwe: CWE-862
```

## API route security matrix (excerpt)

| Route | Method | Auth | Role check | Input val | Rate limit | Risk | Notes |
|---|---|---|---|---|---|---|---|
| /v1/consents/{id} | GET | yes | partial | yes | yes | **Critical** | BOLA (SEC-001) |
| /v1/audit/export | GET | yes | yes | yes | yes | Low | |

## Wrong vs right

**Wrong** — list "use parameterised queries" with no file ref
**Right** — every finding cites file + lines + function

**Wrong** — accept SEC-014 as "code smell"
**Right** — fake security check = High; treat as ASVS V1.4 failure

## Executable acceptance

```bash
tools/sast-scan       --ref v0.7.0-beta.3 --scope control-plane
tools/secret-scan     --ref v0.7.0-beta.3
tools/dep-scan        --ref v0.7.0-beta.3
tools/iac-scan        infra/
opa test              policy/
tools/findings-yaml-check audits/SEC-2026-05-16/findings.yaml
tools/ai-code-risk-scan   --ref v0.7.0-beta.3
cosign verify-blob --signature evidence/audits/SEC-2026-05-16.tar.gz.sig \
  evidence/audits/SEC-2026-05-16.tar.gz
```

## Self-score template

```yaml
self_score: { prompt_id: P-30, overall: 9, blockers: [],
              evidence_refs: ["audits/SEC-2026-05-16/security-report.md",
                              "audits/SEC-2026-05-16/findings.yaml",
                              "audits/SEC-2026-05-16/sbom.json"] }
```
