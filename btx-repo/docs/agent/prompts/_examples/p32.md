# P-32 — Example

## Filled invocation

```yaml
inputs:
  TEST_ID: INT-2026-05-16
  SCOPE: federation
  MATRIX:
    nodes:  [tn-mum, tn-blr, tn-del]
    clouds: [aws, gcp, on-prem]
```

## module-map.yaml (excerpt)

```yaml
modules:
  - name: control-plane
    purpose: "issuance + revocation of consents, policy bundle distribution"
    inputs: ["citizen consent request", "operator policy change"]
    outputs: ["consent grant", "policy bundle"]
    deps: ["trust-node", "audit", "policy-engine"]
    apis_exposed: ["POST /v1/consents", "POST /v1/consents/{id}/revoke"]
    events_published: ["consent.created", "consent.revoked", "policy.bundle.published"]
    events_subscribed: ["trust-node.heartbeat"]
    failure_risks: ["policy bundle stale", "consent revoke lag"]
```

## results.json (excerpt)

```json
{
  "test_id": "INT-2026-05-16",
  "flows": [
    {"id": "F-001", "name": "consent revoke cross-node",
     "result": "pass", "p95_ms": 410, "slo_ms": 500},
    {"id": "F-007", "name": "audit chain replay after partition",
     "result": "fail", "issue": "#902", "owner": "@audit",
     "details": "tn-del missed 3 events after 60s partition; replay incomplete"},
    {"id": "F-013", "name": "cross-cloud KMS failover aws→gcp",
     "result": "pass"}
  ]
}
```

## Wrong vs right

**Wrong** — mock the Trust Node sibling for "cross-node" test
**Right** — run real Trust Node containers; assert chain heads converge after partition heal

**Wrong** — close F-007 as flaky
**Right** — audit chain gap is a hard fail; root-cause and fix before P-28 can pass

## Executable acceptance

```bash
tools/module-map-check    audits/INT-2026-05-16/module-map.yaml
tools/flow-coverage       audits/INT-2026-05-16/integration-plan.md
tools/run-integration     --scope cross-node  --matrix '{"nodes":["tn-mum","tn-blr","tn-del"]}'
tools/run-integration     --scope cross-cloud --matrix '{"clouds":["aws","gcp","on-prem"]}'
tools/chaos-integration   --scope federation
tools/consistency-probe   --scope federation
tools/contract-tests      --scope federation
tools/results-json-check  audits/INT-2026-05-16/results.json
cosign verify-blob --signature evidence/audits/INT-2026-05-16.tar.gz.sig \
  evidence/audits/INT-2026-05-16.tar.gz
```

## Self-score template

```yaml
self_score: { prompt_id: P-32, overall: 9, blockers: [],
              evidence_refs: ["audits/INT-2026-05-16/integration-plan.md",
                              "audits/INT-2026-05-16/module-map.yaml",
                              "audits/INT-2026-05-16/results.json"] }
```
