# P-11 — Example

## Filled invocation

```yaml
inputs:
  NAME: audit-kafka-broker-loss
  TRIGGER: "2/3 Kafka brokers unhealthy in any region"
  SEVERITY: SEV-2
  OWNER: "@sre-lead"
  GD_ID: GD-07
```

## Wrong vs right

**Wrong** — "restart Kafka" with no preconditions / rollback
**Right** — preconditions checked, broker drained, leadership re-elected, audit integrity verified afterwards, rollback path documented if rebuild needed

## Executable acceptance

```bash
tools/runbook-validate docs/runbooks/audit/audit-kafka-broker-loss.md          # all sections present
make chaos GD=GD-07                                                             # scenario completes in staging
tools/runbook-alert-check docs/runbooks/audit/audit-kafka-broker-loss.md       # each trigger linked to alert id
tools/audit-chain-verify --from <ts>                                            # chain intact after drill
```

## Self-score template

```yaml
self_score: { prompt_id: P-11, overall: 9, blockers: [],
              evidence_refs: ["evidence/chaos/GD-07/report.json"] }
```
