# P-27 — Example

## Filled invocation

```yaml
inputs:
  REVIEW_ID: CLV-2026-05-16
  SCOPE: control-plane
  LAYERS: [spec, schema, api, backend, frontend, policy, audit, docs, tests]
```

## drift.yaml (excerpt)

```yaml
drift:
  - id: D-001
    layer_a: api
    layer_b: backend
    severity: critical
    summary: "POST /consents/{id}/revoke documented but handler missing"
    refs:
      - "docs/api/openapi.yaml#/paths/~1consents~1{id}~1revoke"
      - "services/control-plane/routes/consents.go"
    owner: "@control-plane"
    fix_plan: "implement handler + CT-CP-031; ETA 3d"
  - id: D-002
    layer_a: policy
    layer_b: brd
    severity: critical
    summary: "rego rule grants operator read of citizen PII; BRD §5.2 forbids"
    owner: "@security-lead"
    fix_plan: "tighten policy/citizen-pii.rego; add deny-test; risk-accept rejected"
  - id: D-003
    layer_a: audit
    layer_b: actions
    severity: critical
    summary: "consent revocation does not emit audit event"
    fix_plan: "emit consent.revoked per docs/audit/events.yaml; add chain test"
  - id: D-014
    layer_a: docs
    layer_b: reality
    severity: low
    summary: "README mentions deprecated /v1/health endpoint"
```

## Wrong vs right

**Wrong** — close drift D-002 as "operators need this for support"
**Right** — Critical policy drift cannot be risk-accepted silently; either narrow the policy or amend the BRD with privacy-lead sign-off + DPIA delta

**Wrong** — treat doc drift as Critical
**Right** — doc drift defaults to Low unless it misleads operators on safety-critical flow

## Executable acceptance

```bash
tools/openapi-diff       docs/api/openapi.yaml services/control-plane/routes/   # exit 0 or drift row
tools/schema-api-diff    services/control-plane/schema/ docs/api/openapi.yaml
tools/policy-coverage-check policy/ docs/brd/btx-brd.md
tools/audit-coverage-check  docs/audit/events.yaml services/control-plane/handlers
tools/docs-reality-diff  docs/ services/
tools/drift-yaml-check   audits/CLV-2026-05-16/drift.yaml                       # exit 0
cosign verify-blob --signature evidence/audits/CLV-2026-05-16.tar.gz.sig \
  evidence/audits/CLV-2026-05-16.tar.gz                                          # exit 0
```

## Self-score template

```yaml
self_score: { prompt_id: P-27, overall: 9, blockers: [],
              evidence_refs: ["audits/CLV-2026-05-16/cross-layer.md",
                              "audits/CLV-2026-05-16/drift.yaml"] }
```
