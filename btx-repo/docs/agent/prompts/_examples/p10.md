# P-10 — Example

## Filled invocation

```yaml
inputs:
  TITLE: "Adopt SD-JWT for selective disclosure responses"
  PROBLEM: "Citizen-portable flows need minimised, verifier-friendly assertions across wallets."
  DECISION: "Use SD-JWT as the default selective-disclosure format; reserve BBS+ for high-assurance flows."
  ALTERNATIVES:
    - { option: "BBS+ everywhere", why_not: "Tooling immature outside specific stacks" }
    - { option: "Plain VC w/ revealed claims", why_not: "Defeats minimisation" }
  REFS: [BRD-§24, "Annex A §A.9", ADR-017]
  SUPERSEDES: null
```

## Wrong vs right

**Wrong** — edit accepted ADR-007 to add new format
**Right** — open ADR-018 referencing ADR-007 ("complements"), keep ADR-007 unchanged

## Executable acceptance

```bash
tools/adr-validate                                                              # numbering OK
tools/adr-validate --sections                                                  # all template sections present
tools/adr-validate --min-alternatives 2                                        # exit 0
grep -q '0018-' docs/adr/README.md                                              # index updated
```

## Self-score template

```yaml
self_score: { prompt_id: P-10, overall: 9, blockers: [],
              evidence_refs: ["docs/adr/0018-sd-jwt.md", "docs/adr/README.md"] }
```
