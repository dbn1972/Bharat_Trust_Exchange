# P-14 — Example

## Filled invocation

```yaml
inputs:
  MODULE: services/audit-ledger/internal/store
  LOCKIN: "Direct AWS S3 client usage for archival writes; AWS-only IAM assumptions."
  ADAPTERS: [object-store]
  CLOUDS: [aws, azure, gcp, onprem]
```

## Wrong vs right

**Wrong** — feature-flag dual path kept (old S3 + new adapter) "for safety"
**Right** — adapter path only; old code removed in same PR; rollback = revert PR

**Wrong** — semantics changed (default retention shrunk) without ADR
**Right** — preserve semantics; document any intentional change in ADR

## Executable acceptance

```bash
tools/cloud-sdk-leak-check services/audit-ledger/internal/store                # 0 SDK imports in domain/transport
make test MODULE=services/audit-ledger                                          # unchanged green
for c in aws azure gcp onprem; do make certify CLOUD=$c MODULE=services/audit-ledger; done   # exit 0
test -f services/audit-ledger/internal/store/MIGRATION.md                       # exists
```

## Self-score template

```yaml
self_score: { prompt_id: P-14, overall: 9, blockers: [],
              evidence_refs: ["services/audit-ledger/internal/store/MIGRATION.md"] }
```
