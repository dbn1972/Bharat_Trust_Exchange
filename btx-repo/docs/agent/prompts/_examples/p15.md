# P-15 — Example

## Filled invocation

```yaml
inputs:
  SLO: "exchange p95 <= 800 ms at 1000 rps"
  SURFACE: trust-node-shaper
  BASELINE: { p95_ms: 950, rps: 1000 }
  HYPOTHESIS: "Bundle CBOR serialization is allocation-heavy; switching to a pooled encoder reduces GC pressure."
```

## Wrong vs right

**Wrong** — cache PDP decisions for 5 minutes to cut latency (breaks obligation TTL semantics)
**Right** — cache only the immutable parts (policy compile artefact), respect obligation TTL

**Wrong** — disable audit emit on the hot path "temporarily"
**Right** — keep audit; optimise serialization with a pooled writer

## Executable acceptance

```bash
k6 run --thresholds tests/perf/trust-node-shaper.thresholds.json \
  tests/perf/trust-node-shaper/exchange.js                                      # all thresholds green; p95 down >=10%
tools/perf-diff baseline.json result.json                                       # no regression > 10% p95 / > 5% throughput
make certify                                                                     # exit 0
```

## Self-score template

```yaml
self_score: { prompt_id: P-15, overall: 9, blockers: [],
              evidence_refs: ["evidence/perf/trust-node-shaper/v1.7.0/report.html"] }
```
