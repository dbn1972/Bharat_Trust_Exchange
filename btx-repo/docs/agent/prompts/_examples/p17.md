# P-17 — Example

## Filled invocation

```yaml
inputs:
  SPEC_PATH: docs/specs/consent-registry.md
  VERSION: 0.3.0
  RELEASE: v1.7.0
  RISK: medium
```

## Generated PLAN.md (excerpt)

```
PR #1 — bootstrap consent-registry skeleton            -> P-01
PR #2 — POST /v1/consents (grant)                      -> P-02
PR #3 — POST /v1/consents/{id}/revoke                  -> P-02
PR #4 — PurposeGuard rule: consent-grant-flow          -> P-04
PR #5 — audit event consent.granted / revoked          -> P-06
PR #6 — CT-031 consent-revocation-propagation          -> P-08
PR #7 — DPIA + threat model                            -> P-09
PR #8 — perf budget for /v1/consents                    -> P-19 + P-15
PR #9 — release-notes section                          -> P-20
```

## Wrong vs right

**Wrong** — single 4000-LOC PR "implementing the spec"
**Right** — ordered series, each PR <= 1500 LOC / 8 files, atomic, behind feature flag until PR-9

## Executable acceptance

```bash
test -f services/consent-registry/PLAN.md                                       # exists
tools/plan-pr-acceptance services/consent-registry/PLAN.md                     # each PR's prompt Acceptance satisfied
make certify                                                                     # involved CTs green
```

## Self-score template

```yaml
self_score: { prompt_id: P-17, overall: 9, blockers: [],
              evidence_refs: ["services/consent-registry/PLAN.md"] }
```
